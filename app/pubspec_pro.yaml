description: pro version of pubspec.yaml
version: 0.0.1-pro

dependencies:
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.8
  notes_pro:
    path: ../packages/notes/notes_pro

dev_dependencies:
  flutter_lints: ^5.0.0
