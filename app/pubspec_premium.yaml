description: premium version of pubspec.yaml
version: 0.0.1-premium

dependencies:
  cupertino_icons: ^1.0.8
  notes_pro:
    path: ../packages/notes/notes_premium

dev_dependencies:
  flutter_lints: ^5.0.0
