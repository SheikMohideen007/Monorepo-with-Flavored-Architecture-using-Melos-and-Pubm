description: base version of pubspec.yaml version
version: 0.0.1-base

dependencies:
  cupertino_icons: ^1.0.8
  notes_base:
    path: ../packages/notes/notes_base

dev_dependencies:
  flutter_lints: ^5.0.0
